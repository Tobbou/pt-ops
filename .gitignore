node_modules/
dist/
dist-ssr/
*.local
.DS_Store
.vscode/settings.local.json
.claude/settings.local.json
tsconfig.tsbuildinfo

# Rendered pose sheets, and the motion-capture working set: source clips, the 30 MB
# landmark model, extracted landmarks and scratch conversions. All reproducible from
# mocap/README.md, none of it belongs in the repository.
out/
mocap/source/
mocap/models/
mocap/data/
mocap/frames/
mocap/frames.txt
mocap/scratch.ts
